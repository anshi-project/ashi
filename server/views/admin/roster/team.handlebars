<div>
	{{> roster/controls}}	
	{{#each teams}}
	<div class="team-table" data-division="{{this.division}}" data-teamname="{{this.name}}">
		<div class="roster-gm-display">
			<h4>Manager(s):</h4>
				{{#each managers}}
			<h4>
				<a href="/admin/records/manager?id={{this._id}}">
					{{this.fullname}}{{#unless @last}},{{/unless}}
				</a>
			</h4>
			{{/each}}
		</div>
		{{> admin/player_roster}}
		{{> admin/coach_roster}}
	</div>
	{{/each}}
</div>
