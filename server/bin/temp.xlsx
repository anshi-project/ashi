First name, Last name, Do you have a passport?, Passport expiration date, Email, Secondary email, Primary phone number, Secondary phone number, Hometown, Which team are you applying for?, In the past I have coached, The highest level I have coached is, Preferred coaching position, Shirt size (Unisex), Polo size (Unisex), Jacket size (Unisex), Hat size (Unisex), As a coach of Team USA Ball Hockey you will be working with high caliber athletesNaN coach Team USA (300-1000 words), Please describe your coaching style, What makes you a good candidate to coach ball hockey?, One of the challenges of competing with team USA is that the players on the team come from all over the country. Describe how you would go about forming a teamatmosphere knowing you have athletes from all around the country that will only train together for a few weekends before the tournament. (150-500 words), Career highlights, Facebook, Twitter, Instagram, LinkedIn, 
frank, zappa, No, N|A, hotrats@zappa.org, N|A, (485) 737-6239, N|A, West Roxbury, MA, Men's Master's, Ball hockey, Youth Hockey, Assistant Coach for Forwards, XXL, XL, XL, L/XL, 	$(".table tbody tr").each(function(){
		if(mode=="teams" && $(this).data("team") == val){
			$(this).show()
		}		
		else if (mode =="names" && $(this).data("person").match(query)){
			$(this).show()
		}else{
			$(this).hide()
		}
	})	, 	$(".table tbody tr").each(function(){
		if(mode=="teams" && $(this).data("team") == val){
			$(this).show()
		}		
		else if (mode =="names" && $(this).data("person").match(query)){
			$(this).show()
		}else{
			$(this).hide()
		}
	})	, 	$(".table tbody tr").each(function(){
		if(mode=="teams" && $(this).data("team") == val){
			$(this).show()
		}		
		else if (mode =="names" && $(this).data("person").match(query)){
			$(this).show()
		}else{
			$(this).hide()
		}
	})	, 	$(".table tbody tr").each(function(){
		if(mode=="teams" && $(this).data("team") == val){
			$(this).show()
		}		
		else if (mode =="names" && $(this).data("person").match(query)){
			$(this).show()
		}else{
			$(this).hide()
		}
	})	, N|A, N|A, N|A, N|A, N|A, 
